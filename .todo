Features:

Bugs:

Codebase Improvement:
    ☐ Fix CI
        ☐ Investigate why BasedPyright (and Pyright) doesn't find `Pytest` and `hatchling` packages
        ☐ Investigate the cache issue with `uv` on `windows`
    ☐ Fix readme badges (build)

Tests:

Final:
    ☐ Add documentation
